import { } from '../types'

const initialState = {
  loading: false,
  errors: null,
  recievedNavChildren: false
}

export default function(state = initialState, action){
  switch(action.type){
    default:
      return state
  }
}